{
    "tasks": [
        {
            "label": "build debug meson",
            "type": "shell",
            "command": "meson build --buildtype=debug && ninja -C build"
        }
    ],
    "version": "2.0.0"
}
